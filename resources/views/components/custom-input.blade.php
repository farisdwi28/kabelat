<div class="mb-4">
    <label for="{{ $name }}" class="block text-gray-700 font-semibold">{{ $label }}<span class="text-red-500">*</span></label>
    <input type="{{ $type }}" id="{{ $name }}" name="{{ $name }}" class="w-full border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-primary" placeholder="Masukkan {{ $label }} Anda" autocomplete="off">
</div>
